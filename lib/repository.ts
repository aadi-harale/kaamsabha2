"use client";
import { AppState,initialState } from "./domain";const STORAGE_KEY="kaamsabha2:state:v1",SESSION_KEY="kaamsabha2:session:v1";export interface StateRepository{load():AppState;save(next:AppState):void;clearSession():void}function isState(value:unknown):value is AppState{if(!value||typeof value!=="object")return false;const v=value as Partial<AppState>;return v.schema===1&&Array.isArray(v.workers)&&Array.isArray(v.jobs)&&Array.isArray(v.receipts)}function normalizeState(value:AppState):AppState{const seed=initialState();return{...seed,...value,cooperatives:Array.isArray(value.cooperatives)?value.cooperatives:seed.cooperatives,workers:Array.isArray(value.workers)?value.workers:seed.workers,jobs:Array.isArray(value.jobs)?value.jobs.map(j=>({...j,evidence:Array.isArray(j.evidence)?j.evidence:[],changeOrders:Array.isArray(j.changeOrders)?j.changeOrders:[]})):[],receipts:Array.isArray(value.receipts)?value.receipts.map(r=>({...r,federationReason:r.federationReason||"Legacy receipt: cooperative routing reason was not recorded."})):[],settlements:Array.isArray(value.settlements)?value.settlements:[],feedback:Array.isArray(value.feedback)?value.feedback:[],challenges:Array.isArray(value.challenges)?value.challenges:[],issues:Array.isArray(value.issues)?value.issues:[],votes:Array.isArray(value.votes)?value.votes:[],session:null}}
export class BrowserStateRepository implements StateRepository{load():AppState{if(typeof window==="undefined")return initialState();try{const raw=window.localStorage.getItem(STORAGE_KEY),parsed:unknown=raw?JSON.parse(raw):null,base=isState(parsed)?normalizeState(parsed):initialState(),sessionRaw=window.sessionStorage.getItem(SESSION_KEY),session=sessionRaw?JSON.parse(sessionRaw) as AppState["session"]:null;return{...base,session}}catch{this.resetCorruptState();return initialState()}}save(next:AppState){if(typeof window==="undefined")return;window.localStorage.setItem(STORAGE_KEY,JSON.stringify({...next,session:null}));if(next.session)window.sessionStorage.setItem(SESSION_KEY,JSON.stringify(next.session));else window.sessionStorage.removeItem(SESSION_KEY)}clearSession(){if(typeof window!=="undefined")window.sessionStorage.removeItem(SESSION_KEY)}private resetCorruptState(){if(typeof window==="undefined")return;window.localStorage.removeItem(STORAGE_KEY);window.sessionStorage.removeItem(SESSION_KEY)}}export const stateRepository=new BrowserStateRepository();
