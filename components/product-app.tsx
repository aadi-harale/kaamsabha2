"use client";

import { FormEvent, useEffect, useMemo, useState } from "react";
import {
  addEvidence, addFeedback, addIssueNote, cancelBookingProtected, castVote, createBooking,
  declineJobSafely, openIssue, recordOtpFailure, recordOtpIssued, recordOtpVerified,
  reviewIssue, reviewPolicyImpact, reviewPolicySuggestion, settleJob, signIn, signOut,
  submitPolicySuggestion, transitionJob, updateWorkability
} from "@/lib/commands";
import type { AppState, Locale, OtpChallenge, Role, SuggestionCategory, Worker } from "@/lib/domain";
import { initialState, workerDailyLimit } from "@/lib/domain";
import { decidePreStartScopeChange, proposePreStartScopeChange } from "@/lib/prestart-scope";
import { copy } from "@/lib/i18n";
import { stateRepository } from "@/lib/repository";
import { ServiceMap } from "@/components/service-map";
import { FederationExchange } from "@/components/federation-exchange";
import { AdminGovernance } from "@/components/admin-governance";
import { AdminReplayCourt, WorkerReplayCourt } from "@/components/replay-court";
import { DemoPaymentSheet } from "@/components/demo-payment-sheet";

type Run=(fn:()=>AppState,message?:string)=>void;
type DemoOtp={jobId:string;purpose:"start"|"completion";code:string}|null;

const roles:{id:Role;title:string;note:string;demo:string}[]=[
  {id:"customer",title:"Customer",note:"Book and track trusted local services",demo:"customer01"},
  {id:"worker",title:"Worker member",note:"Work, protections, voice and governance",demo:"W02"},
  {id:"admin",title:"Cooperative admin",note:"Operations, cases, policy and federation",demo:"admin01"},
];
const localeNames:Record<Locale,string>={en:"English",hi:"हिन्दी",mr:"मराठी"};
const serviceCatalog=[
  {id:"electrician",name:"Electrical",icon:"⚡",note:"Repairs, fittings & safety checks",price:760},
  {id:"cleaning",name:"Home cleaning",icon:"✦",note:"Rooms, kitchen & deep clean",price:760},
  {id:"appliance",name:"Appliance repair",icon:"⌁",note:"Diagnosis & repair visit",price:760},
  {id:"plumbing",name:"Plumbing",icon:"◒",note:"Leaks, taps, fittings & water issues",price:760},
  {id:"carpentry",name:"Carpentry",icon:"◇",note:"Furniture, doors, shelves & repairs",price:760},
] as const;
const labels:Record<string,string>={requested:"Finding a member",assigned:"Worker assigned",accepted:"Accepted",travelling:"On the way",arrived:"Arrived · confirm scope",started:"Work in progress",change_pending:"Waiting for scope approval",completed:"Work completed",settled:"Paid & settled",cancelled:"Cancelled"};
function pretty(value:string){return value.replaceAll("_"," ").replaceAll("-"," ").replace(/\b\w/g,c=>c.toUpperCase());}

export function ProductApp(){
  const[state,setState]=useState<AppState>(initialState());
  const[ready,setReady]=useState(false),[tab,setTab]=useState("home"),[error,setError]=useState(""),[notice,setNotice]=useState(""),[demoOtp,setDemoOtp]=useState<DemoOtp>(null);
  useEffect(()=>{const local=stateRepository.load();setState(local);setReady(true);void stateRepository.loadRemote().then(remote=>{if(remote&&remote.revision>local.revision)setState({...remote,session:local.session});});},[]);
  useEffect(()=>{if(ready)stateRepository.save(state);},[state,ready]);
  useEffect(()=>{document.documentElement.lang=state.locale;},[state.locale]);
  const role=state.session?.role,t=copy[state.locale];
  const worker=role==="worker"?state.workers.find(w=>w.id===state.session?.userId):undefined;
  const visibleJobs=useMemo(()=>role==="customer"?state.jobs.filter(j=>j.customerId===state.session?.userId):role==="worker"?state.jobs.filter(j=>j.workerId===worker?.id):state.jobs,[role,state.jobs,state.session?.userId,worker?.id]);
  const activeJob=visibleJobs.find(j=>!["settled","cancelled"].includes(j.status)),latestJob=visibleJobs[0];
  const run:Run=(fn,message)=>{try{setError("");setNotice("");setState(fn());if(message)setNotice(message);}catch(e){setError(e instanceof Error?e.message:"Action failed");}};

  async function issueOtp(jobId:string,purpose:"start"|"completion"){
    setError("");
    try{
      const response=await fetch("/api/otp",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"issue",jobId,purpose})});
      const data=await response.json() as {token?:string;expiresAt?:number;attemptsLeft?:number;demoCode?:string;error?:string};
      if(!response.ok||!data.token||!data.expiresAt)throw new Error(data.error||"OTP could not be issued");
      const challenge:OtpChallenge={purpose,token:data.token,expiresAt:data.expiresAt,attemptsLeft:data.attemptsLeft??5};
      setState(s=>recordOtpIssued(s,jobId,challenge));
      setDemoOtp(data.demoCode?{jobId,purpose,code:data.demoCode}:null);
      setNotice(data.demoCode?`Demo OTP: ${data.demoCode}`:"OTP issued securely.");
    }catch(e){setError(e instanceof Error?e.message:"OTP could not be issued");}
  }

  async function verifyOtp(jobId:string,purpose:"start"|"completion",code:string){
    setError("");
    try{
      const job=state.jobs.find(j=>j.id===jobId),challenge=purpose==="start"?job?.startOtp:job?.completionOtp;
      if(!challenge)throw new Error("Customer has not issued this OTP");
      const response=await fetch("/api/otp",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"verify",jobId,purpose,token:challenge.token,code})});
      const data=await response.json() as {ok?:boolean;reason?:string};
      if(!response.ok||!data.ok){setState(s=>recordOtpFailure(s,jobId,purpose));throw new Error(data.reason==="expired"?"OTP expired":"OTP incorrect or invalid");}
      setState(s=>recordOtpVerified(s,jobId,purpose));
      setNotice(purpose==="start"?"Work started with customer confirmation.":"Completion confirmed. Customer can settle the invoice.");
    }catch(e){setError(e instanceof Error?e.message:"OTP verification failed");}
  }

  if(!ready)return <main className="shell loading">Loading KaamSabha…</main>;
  if(!state.session)return <Auth state={state} run={run} error={error}/>;
  const nav=role==="customer"?[{id:"home",label:t.home,icon:"⌂"},{id:"orders",label:t.orders,icon:"▤"},{id:"support",label:t.help,icon:"?"}]:role==="worker"?[{id:"home",label:t.current,icon:"▣"},{id:"fair",label:t.fair,icon:"◎"},{id:"issues",label:t.issues,icon:"!"},{id:"suggestions",label:t.suggestions,icon:"✎"},{id:"earnings",label:t.earnings,icon:"₹"},{id:"governance",label:t.governance,icon:"◫"}]:[{id:"home",label:t.operations,icon:"▦"},{id:"workers",label:t.workers,icon:"◉"},{id:"issues",label:t.cases,icon:"!"},{id:"suggestions",label:t.suggestions,icon:"✎"},{id:"settlements",label:t.money,icon:"₹"},{id:"governance",label:t.governance,icon:"◫"},{id:"demand",label:t.demand,icon:"↗"},{id:"federation",label:t.federation,icon:"⌘"}];

  return <main className="shell productShell"><a className="skipLink" href="#workspace">Skip to main content</a><header className="topbar"><button className="brand brandButton" onClick={()=>setTab("home")} aria-label="KaamSabha home"><span className="brandMark small">K</span><span><strong>KaamSabha</strong><small>Worker-owned local services</small></span></button><div className="topContext"><span className="locationPill">⌖ Pune cooperative network</span><span className="rolePill">{roles.find(r=>r.id===role)?.title}</span></div><div className="topActions"><label className="locale"><span className="srOnly">{t.language}</span><select value={state.locale} onChange={e=>setState(s=>({...s,locale:e.target.value as Locale,revision:s.revision+1}))}>{Object.entries(localeNames).map(([id,name])=><option value={id} key={id}>{name}</option>)}</select></label><button className="ghost compact" onClick={()=>run(()=>signOut(state))}>{t.logout}</button></div></header><div className="appGrid"><aside className="sideNav"><div className="identity"><span className={`roleDot ${role}`}/><div><strong>{role==="worker"?worker?.name:state.session.userId}</strong><small>{roles.find(r=>r.id===role)?.title}</small></div></div><nav aria-label="Primary navigation">{nav.map(item=><button key={item.id} className={tab===item.id?"navItem active":"navItem"} aria-current={tab===item.id?"page":undefined} onClick={()=>setTab(item.id)}><span className="navIcon" aria-hidden="true">{item.icon}</span><span>{item.label}</span></button>)}</nav><div className="constitution"><small>ACTIVE CONSTITUTION</small><strong>{state.policy.version}</strong><span>₹{state.policy.minimumPayout} protection floor</span></div></aside><section id="workspace" className="workspace" tabIndex={-1}><div className="pageHead compactHead"><div><p className="eyebrow">{role==="customer"?"TRUSTED LOCAL SERVICES":role==="worker"?"MEMBER WORKSPACE":"COOPERATIVE OPERATIONS"}</p><h1>{role==="customer"?"Services for your home":role==="worker"?`${worker?.name??"Member"}, your work and voice`:"Cooperative operations"}</h1></div>{role!=="customer"&&<span className="statusChip"><span className="statusDot"/>Shared register · rev {state.revision}</span>}</div>{notice&&<p className="notice" role="status">{notice}</p>}{error&&<p className="error" role="alert">{error}</p>}
    {role==="customer"&&tab==="home"&&<CustomerHome state={state} run={run} openOrders={()=>setTab("orders")}/>} {role==="customer"&&tab==="orders"&&<CustomerOrders state={state} jobs={visibleJobs} run={run} issueOtp={issueOtp} demoOtp={demoOtp}/>} {role==="customer"&&tab==="support"&&<CustomerSupport state={state} job={activeJob??latestJob} run={run}/>} 
    {role==="worker"&&tab==="home"&&worker&&<WorkerToday state={state} job={activeJob} worker={worker} run={run} verifyOtp={verifyOtp}/>} {role==="worker"&&tab==="fair"&&worker&&<WorkerFair state={state} worker={worker} run={run}/>} {role==="worker"&&tab==="issues"&&worker&&<WorkerIssues state={state} worker={worker} job={activeJob??latestJob} run={run}/>} {role==="worker"&&tab==="suggestions"&&worker&&<WorkerSuggestions state={state} worker={worker} run={run}/>} {role==="worker"&&tab==="earnings"&&worker&&<WorkerEarnings state={state} worker={worker}/>} {role==="worker"&&tab==="governance"&&worker&&<WorkerGovernance state={state} worker={worker} run={run}/>} 
    {role==="admin"&&<Admin state={state} tab={tab} run={run}/>} {role!=="customer"&&<RuleCompare state={state}/>}</section></div></main>;
}

function Auth({state,run,error}:{state:AppState;run:Run;error:string}){return <main className="authShell"><section className="authCard"><div className="authIntro"><span className="brandMark">K</span><p className="eyebrow light">SIH26089 · Cooperative services</p><h1>Reliable local help.<br/>Fair work behind it.</h1><p>A familiar home-services marketplace for customers, with worker-owned rules, protections and auditable decisions underneath.</p><div className="trustRow"><span>✓ Verified skills</span><span>✓ In-browser maps</span><span>✓ Worker-owned rules</span></div><div className="authProof"><div><strong>5</strong><span>service categories</span></div><div><strong>₹{state.policy.minimumPayout}</strong><span>protected worker floor</span></div><div><strong>3</strong><span>languages</span></div></div></div><form className="rolePicker" onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);run(()=>signIn(state,String(d.get("userId")),String(d.get("role")) as Role));}}><div><p className="eyebrow">JUDGE DEMO</p><h2>Choose a workspace</h2><p>All roles share one persisted register.</p></div>{roles.map((r,i)=><label className="roleChoice" key={r.id}><input type="radio" name="role" value={r.id} defaultChecked={i===0} onChange={e=>{const input=e.currentTarget.form?.elements.namedItem("userId") as HTMLInputElement;if(input)input.value=r.demo;}}/><span><strong>{r.title}</strong><small>{r.note}</small></span></label>)}<label className="field"><span>Demo ID</span><input name="userId" defaultValue="customer01"/></label><button>Open workspace →</button>{error&&<p className="error">{error}</p>}<p className="formFoot">Deterministic seed 26089 · local-first demo register</p></form></section></main>}

function CustomerHome({state,run,openOrders}:{state:AppState;run:Run;openOrders:()=>void}){
  const[selected,setSelected]=useState<string|null>(null),[query,setQuery]=useState(""),[problem,setProblem]=useState(""),[aiBusy,setAiBusy]=useState(false),[ai,setAi]=useState<{summary:string;urgency:string;safetyNote:string;notice:string}|null>(null);
  const active=state.jobs.find(j=>j.customerId===state.session?.userId&&!["settled","cancelled"].includes(j.status));
  const chosen=serviceCatalog.find(s=>s.id===selected),filtered=serviceCatalog.filter(s=>`${s.name} ${s.note}`.toLowerCase().includes(query.toLowerCase()));
  const availability=(service:string)=>state.workers.filter(w=>w.verified&&w.active&&w.available&&w.skills.includes(service)&&w.workloadTodayMinutes<workerDailyLimit(w)).length;
  async function assist(){if(!chosen||!problem.trim())return;setAiBusy(true);try{const response=await fetch("/api/ai/intake",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({service:chosen.id,text:problem})});const data=await response.json() as {summary:string;urgency:string;safetyNote:string;notice:string};if(response.ok){setAi(data);setProblem(data.summary);}}finally{setAiBusy(false);}}
  function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!chosen)return;const d=new FormData(e.currentTarget),when=new Date(),schedule=String(d.get("schedule"));if(schedule==="2h")when.setHours(when.getHours()+2);if(schedule==="tomorrow")when.setDate(when.getDate()+1);const file=d.get("reference") as File|null;run(()=>createBooking(state,{customerId:state.session!.userId,service:chosen.id,locality:String(d.get("location")),scheduledAt:when.toISOString(),emergency:d.get("emergency")==="on",amount:chosen.price,intakeNote:problem,referenceName:file?.name??""}),"Booking confirmed and dispatched under the cooperative constitution.");setSelected(null);setProblem("");setAi(null);}
  return <>{active&&<section className="activeOrderCard dense"><div className="activeOrderIcon">⌁</div><div><p className="eyebrow">ACTIVE BOOKING</p><strong>{pretty(active.service)} · {labels[active.status]}</strong><span>{active.workerId?`Member ${active.workerId} assigned`:"Finding a certified member"} · {active.locality}</span></div><button className="secondary" onClick={openOrders}>Track order</button></section>}<section className="ucHero"><div><p className="eyebrow">HOUSEHOLD & COMMUNITY SERVICES</p><h2>What do you need help with?</h2><p>Verified local professionals, clear scope, and worker-protected pricing.</p></div><label className="ucSearch"><span aria-hidden="true">⌕</span><span className="srOnly">Search services</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search services"/></label></section><div className="ucSectionHead"><div><h2>Popular services</h2><p>Choose a category to see availability and book.</p></div><span>● Live cooperative capacity</span></div><section className="ucServiceGrid" aria-label="Service categories">{filtered.map(s=>{const available=availability(s.id);return <button key={s.id} className={selected===s.id?"ucServiceCard selected":"ucServiceCard"} onClick={()=>setSelected(s.id)} aria-pressed={selected===s.id}><span className="ucServiceIcon">{s.icon}</span><span className="ucServiceBody"><strong>{s.name}</strong><span>{s.note}</span><span className="ucServiceTags"><small>From ₹{Math.max(s.price,state.policy.minimumPayout)}</small><small className={available?"available":"constrained"}>{available?`${available} safe member${available===1?"":"s"} now`:"Federation may assist"}</small></span></span><span className="ucChevron" aria-hidden="true">›</span></button>})}</section>{chosen&&<section className="bookingConfigurator ucBooking"><div className="ucBookingHead"><div><p className="eyebrow">BOOK {chosen.name.toUpperCase()}</p><h2>Confirm the details</h2><p>Describe the problem once. That scope follows the job and protects both sides.</p></div><button className="ghost compact" onClick={()=>setSelected(null)} aria-label="Close booking form">×</button></div><form className="bookingForm" onSubmit={submit}><div className="ucBookingGrid"><label className="field"><span>Service location</span><input name="location" defaultValue="Kharadi, Pune" required/></label><label className="field"><span>When?</span><select name="schedule" defaultValue="now"><option value="now">Now / on-demand</option><option value="2h">In about 2 hours</option><option value="tomorrow">Tomorrow</option></select></label></div><div className="intakeCard"><label className="field"><span>What is the problem?</span><textarea value={problem} onChange={e=>setProblem(e.target.value)} placeholder="Example: switch sparks when turned on…" required/></label><button type="button" className="secondary" disabled={aiBusy||!problem.trim()} onClick={()=>void assist()}>{aiBusy?"Structuring…":"Help me structure this"}</button>{ai&&<div className="aiAssistResult"><span className="microTag">{ai.urgency}</span><strong>Safety note</strong><span>{ai.safetyNote}</span><small>{ai.notice}</small></div>}</div><div className="ucBookingGrid"><label className="fileField"><span>Reference photo (optional)</span><input type="file" name="reference" accept="image/*"/></label><label className="checkField"><input type="checkbox" name="emergency"/><span><strong>Emergency priority</strong><small>Use only for urgent needs.</small></span></label></div><div className="bookingTotal compactTotal"><span>Protected service amount</span><strong>₹{Math.max(chosen.price,state.policy.minimumPayout)}</strong><small>No reverse bidding. Federation cannot lower this protection.</small></div><button>Confirm booking</button></form></section>}<section className="compactAssurances"><span>✓ Scope Lock</span><span>₹ Protected payout</span><span>◎ Explainable dispatch</span><span>! Rating firewall</span></section></>;
}

function CustomerOrders({state,jobs,run,issueOtp,demoOtp}:{state:AppState;jobs:AppState["jobs"];run:Run;issueOtp:(id:string,p:"start"|"completion")=>Promise<void>;demoOtp:DemoOtp}){
  const[view,setView]=useState<"active"|"past">("active"),shown=jobs.filter(j=>view==="active"?!["settled","cancelled"].includes(j.status):["settled","cancelled"].includes(j.status));
  return <section className="ordersPage"><div className="sectionTitle roomy"><div><h2>Bookings</h2><p className="muted">Track one clear path from assignment to invoice.</p></div><div className="segment"><button className={view==="active"?"active":""} onClick={()=>setView("active")}>Active</button><button className={view==="past"?"active":""} onClick={()=>setView("past")}>Past</button></div></div>{shown.length?shown.map(j=>{
    const worker=state.workers.find(w=>w.id===j.workerId),pending=j.changeOrders.find(c=>c.approved===undefined),settlement=state.settlements.find(s=>s.jobId===j.id),cancellation=state.cancellations.find(c=>c.jobId===j.id),latestDecision=[...j.changeOrders].reverse().find(c=>c.approved!==undefined);
    return <article className="orderCard" key={j.id}><div className="orderTop"><div><small>{j.id} · {new Date(j.scheduledAt).toLocaleString()}</small><h3>{pretty(j.service)}</h3><p>{j.locality}</p></div><span className="statusChip">{labels[j.status]}</span></div>{j.workerId&&!["settled","cancelled"].includes(j.status)&&<ServiceMap job={j} workerName={worker?.name}/>} {j.federationOpportunityId&&<div className="customerFederationNote"><span className="microTag">Federation assisted</span><strong>Another cooperative had safe capacity.</strong><span>Federation selected the cooperative first; that cooperative selected its own member. Worker protection stayed unchanged.</span></div>}<div className="orderFacts"><div><span>Worker</span><strong>{worker?.name??"Pending"}</strong></div><div><span>Current approved amount</span><strong>₹{j.amount}</strong></div><div><span>Work proof</span><strong>{j.evidence.length}</strong></div></div>{j.intakeNote&&<div className="intakeSummary"><strong>Booked scope</strong><span>{j.intakeNote}</span></div>}{["requested","assigned","accepted","travelling"].includes(j.status)&&<button className="dangerGhost" onClick={()=>run(()=>cancelBookingProtected(state,j.id,"Customer no longer needs service"),"Cancellation recorded with worker-protection rules.")}>Cancel booking</button>}
    {pending&&<div className="scopePendingBanner"><small>SCOPE LOCK · YOUR APPROVAL IS REQUIRED BEFORE WORK STARTS</small><strong>{pending.description} · +₹{pending.amountDelta}</strong><span>The worker cannot start this added work until you approve it. If declined, the original booked scope remains unchanged.</span><div className="actions"><button onClick={()=>run(()=>decidePreStartScopeChange(state,j.id,pending.id,true),"Additional scope approved. Please issue a fresh start OTP when ready.")}>Approve addition</button><button className="secondary" onClick={()=>run(()=>decidePreStartScopeChange(state,j.id,pending.id,false),"Additional scope declined. Original scope remains locked.")}>Keep original scope</button></div></div>}
    {j.status==="arrived"&&!pending&&<div className="scopeDecisionNote"><span>✓</span><div><strong>Scope is settled before work starts.</strong><span>{latestDecision?.approved===true?`Approved addition: ${latestDecision.description}. New total ₹${j.amount}.`:latestDecision?.approved===false?"The requested addition was declined; the original scope remains active.":"No extra scope is pending."}</span></div></div>}
    {j.status==="arrived"&&!pending&&<button onClick={()=>void issueOtp(j.id,"start")}>Issue start OTP</button>}
    {demoOtp?.jobId===j.id&&demoOtp.purpose==="start"&&<p className="otpCallout">Start OTP <strong>{demoOtp.code}</strong><span>Share only after the scope shown above is correct.</span></p>}
    {j.status==="started"&&j.evidence.length>0&&<button onClick={()=>void issueOtp(j.id,"completion")}>Approve proof & issue completion OTP</button>}
    {demoOtp?.jobId===j.id&&demoOtp.purpose==="completion"&&<p className="otpCallout">Completion OTP <strong>{demoOtp.code}</strong><span>Confirms customer-reviewed work.</span></p>}
    {j.status==="completed"&&<DemoPaymentSheet amount={j.amount} jobId={j.id} onSettle={()=>run(()=>settleJob(state,j.id),"Demo payment completed; invoice and protected payout posted.")}/>} 
    {settlement&&<div className="invoiceStrip"><strong>{settlement.invoiceNumber}</strong><span>Worker ₹{settlement.workerPayout}</span><span>Fee ₹{settlement.platformFee}</span></div>}{cancellation&&<div className="cancelProtection"><strong>Cancellation protection ₹{cancellation.workerPayout}</strong><span>Customer refund ₹{cancellation.customerRefund}</span></div>}{j.status==="settled"&&!state.feedback.some(f=>f.jobId===j.id)&&<FeedbackForm state={state} jobId={j.id} run={run}/>}</article>;
  }):<Empty text={view==="active"?"No active bookings.":"No past orders yet."}/>}</section>;
}

function FeedbackForm({state,jobId,run}:{state:AppState;jobId:string;run:Run}){const[rating,setRating]=useState(5),[note,setNote]=useState("");return <form className="feedbackComposer" onSubmit={e=>{e.preventDefault();run(()=>addFeedback(state,jobId,rating,note||"Customer feedback submitted"),rating<=2?"Rating Firewall opened human review; worker access is unchanged.":"Feedback recorded.");}}><label className="field"><span>Rating</span><select value={rating} onChange={e=>setRating(Number(e.target.value))}><option value={5}>5 — Excellent</option><option value={4}>4 — Good</option><option value={3}>3 — Okay</option><option value={2}>2 — Needs review</option><option value={1}>1 — Serious concern</option></select></label><label className="field"><span>Optional note</span><input value={note} onChange={e=>setNote(e.target.value)} placeholder="What should the cooperative know?"/></label><button>Submit feedback</button></form>}
function CustomerSupport({state,job,run}:{state:AppState;job?:AppState["jobs"][number];run:Run}){return <><section className="supportHero"><div><p className="eyebrow">CUSTOMER SUPPORT</p><h2>Report a problem without automatically punishing a worker.</h2><p>Issues enter cooperative review. Ratings and complaints never auto-deactivate a member.</p></div><span className="supportBadge">Rating firewall active</span></section><section className="supportGrid">{["Scope / extra charge","Safety","Payment / invoice","Service quality"].map(category=><button className="supportCard" key={category} onClick={()=>run(()=>openIssue(state,state.session!.userId,category,job?.id),"Issue added to the cooperative case register.")}><strong>{category}</strong><span>Create a reviewable record.</span><small>{job?`Links to ${job.id}`:"General support"} →</small></button>)}</section></>}

function WorkerToday({state,job,worker,run,verifyOtp}:{state:AppState;job?:AppState["jobs"][number];worker:Worker;run:Run;verifyOtp:(id:string,p:"start"|"completion",code:string)=>Promise<void>}){
  const[declineReason,setDeclineReason]=useState("Safety concern"),[scopeText,setScopeText]=useState("Additional work requested on-site"),[scopeAmount,setScopeAmount]=useState(180);
  if(!job)return <><Empty text="No active task. Your availability and safety limits remain under your control."/><Workability state={state} worker={worker} run={run}/></>;
  const submitOtp=(purpose:"start"|"completion")=>(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();void verifyOtp(job.id,purpose,String(new FormData(e.currentTarget).get("code")));};
  const pending=job.changeOrders.find(c=>c.approved===undefined);
  const decided=[...job.changeOrders].reverse().find(c=>c.approved!==undefined);
  return <><section className="jobHero"><div><p className="eyebrow light">CURRENT JOB · {job.id}</p><h2>{pretty(job.service)} in {job.locality}</h2><div className="jobMeta"><span>{job.emergency?"Emergency":"Scheduled"}</span><span>Scope locked</span><span>{state.cooperatives.find(c=>c.id===job.cooperativeId)?.locality??job.cooperativeId}</span></div></div><div className="payout"><small>PROTECTED PAYOUT</small><strong>₹{job.amount}</strong><span>₹87 estimated costs · ₹{Math.max(0,job.amount-87)} estimated net</span></div></section><ServiceMap job={job} workerName={worker.name} mode="worker"/><section className="workerGrid"><article className="panel taskPanel"><div className="sectionTitle"><div><p className="eyebrow">TASK STATUS</p><h2>{labels[job.status]}</h2></div></div>{job.intakeNote&&<div className="scopeBox"><small>BOOKED SCOPE</small><strong>{job.intakeNote}</strong></div>}{job.status==="assigned"&&<><button onClick={()=>run(()=>transitionJob(state,job.id,"accepted"),"Job accepted.")}>Accept job</button><div className="safeDecline"><strong>Can’t safely take this job?</strong><span>Declining before acceptance has zero rating or opportunity penalty.</span><label className="field"><span>Reason</span><select value={declineReason} onChange={e=>setDeclineReason(e.target.value)}><option>Safety concern</option><option>Out of booked scope</option><option>Schedule conflict</option><option>Service area conflict</option><option>Other workability reason</option></select></label><button className="secondary" onClick={()=>run(()=>declineJobSafely(state,job.id,declineReason),"Safe decline recorded with zero penalty; dispatch reran.")}>Safely decline</button></div></>}{job.status==="accepted"&&<button onClick={()=>run(()=>transitionJob(state,job.id,"travelling"))}>Start travel</button>}{job.status==="travelling"&&<button onClick={()=>run(()=>transitionJob(state,job.id,"arrived"))}>Mark arrived</button>}
    {job.status==="arrived"&&<div className="preStartScope"><div className="preStartScopeHeader"><div><p className="eyebrow">SCOPE CHECK · BEFORE START OTP</p><h3>Did the customer ask for anything beyond the booking?</h3><p>If yes, record it now. The customer must approve the addition before you can start that added work.</p></div><span className="statusChip">Original scope locked</span></div><div className="scopeFields"><label className="field"><span>Additional work</span><input value={scopeText} onChange={e=>setScopeText(e.target.value)} placeholder="Example: replace one extra switch"/></label><label className="field"><span>Added amount</span><input type="number" min={0} step={10} value={scopeAmount} onChange={e=>setScopeAmount(Number(e.target.value))}/></label></div><div className="scopePreview"><span>If approved, customer total becomes</span><strong>₹{job.amount+Math.max(0,scopeAmount||0)}</strong></div><button className="secondary" onClick={()=>run(()=>proposePreStartScopeChange(state,job.id,scopeText,scopeAmount),"Scope addition sent to the customer. Work stays blocked until they decide.")}>Send addition for approval</button><div className="scopeDecisionNote"><span>✓</span><div><strong>No extra work requested?</strong><span>Ask the customer to issue the start OTP and begin only the locked scope.</span></div></div>{decided&&<div className="scopeDecisionNote"><span>{decided.approved?"✓":"↩"}</span><div><strong>{decided.approved?"Customer approved the latest addition":"Customer kept the original scope"}</strong><span>{decided.description}{decided.approved?` · +₹${decided.amountDelta}`:""}</span></div></div>}<form className="otpForm" onSubmit={submitOtp("start")}><label className="field"><span>Start OTP</span><input name="code" inputMode="numeric" maxLength={6} required/></label><button>Verify OTP & start work</button></form></div>}
    {job.status==="change_pending"&&pending&&<div className="scopePendingBanner"><small>SCOPE LOCK · WAITING FOR CUSTOMER</small><strong>{pending.description} · +₹{pending.amountDelta}</strong><span>Do not start the additional work yet. Any previously issued start OTP was invalidated when this scope request was created.</span></div>}
    {job.status==="started"&&<><div className="actions taskActions"><button onClick={()=>run(()=>addEvidence(state,job.id,"Before/after work proof"))}>Add work proof</button></div><form className="otpForm" onSubmit={submitOtp("completion")}><label className="field"><span>Completion OTP</span><input name="code" inputMode="numeric" maxLength={6} required/></label><button>Verify completion</button></form></>}</article><article className="panel fairSummary"><p className="eyebrow">YOUR PROTECTIONS</p><h2>Before you commit</h2><ul className="checkList"><li><span>✓</span> Certified skill required before ranking</li><li><span>✓</span> ₹{state.policy.minimumPayout} protected floor</li><li><span>✓</span> Safe decline = zero penalty</li><li><span>✓</span> Extra scope needs customer approval before start</li><li><span>✓</span> Complaints require human review</li></ul></article></section><Workability state={state} worker={worker} run={run}/></>;
}

function Workability({state,worker,run}:{state:AppState;worker:Worker;run:Run}){const[available,setAvailable]=useState(worker.available),[limit,setLimit]=useState(worker.maxDailyMinutes??480),[rest,setRest]=useState(worker.minRestMinutes??30);return <section className="panel workability"><div><p className="eyebrow">WORKABILITY & SAFETY</p><h2>Set limits before dispatch sees you as available.</h2></div><div className="workabilityForm"><label className="checkField"><input type="checkbox" checked={available} onChange={e=>setAvailable(e.target.checked)}/><span><strong>Available for new work</strong><small>Turn off without rating penalty.</small></span></label><label className="field"><span>Maximum work today</span><select value={limit} onChange={e=>setLimit(Number(e.target.value))}><option value={360}>6 hours</option><option value={420}>7 hours</option><option value={480}>8 hours</option></select></label><label className="field"><span>Minimum rest gap</span><select value={rest} onChange={e=>setRest(Number(e.target.value))}><option value={30}>30 minutes</option><option value={60}>60 minutes</option><option value={90}>90 minutes</option></select></label><button onClick={()=>run(()=>updateWorkability(state,{available,maxDailyMinutes:limit,minRestMinutes:rest}),"Safety limits saved. Future dispatch checks use them first.")}>Save safety limits</button></div></section>}
function WorkerFair({state,worker,run}:{state:AppState;worker:Worker;run:Run}){const receipts=state.receipts.filter(r=>r.workerId===worker.id||r.candidateWorkerIds?.includes(worker.id)||r.candidateSnapshot?.some(c=>c.workerId===worker.id)),opportunities=state.opportunities.filter(o=>o.workerId===worker.id);return <><section className="fairHero"><div><p className="eyebrow">FAIR WORK</p><h2>Your offers, decisions and right to challenge them.</h2><p>Only real eligible offers count toward opportunity access. Safe declines remain penalty 0. Every frozen allocation can be inspected through Replay Court.</p></div><div className="fairSeal"><span>Decision records</span><strong>{receipts.length}</strong><small>{opportunities.length} valid offer(s)</small></div></section><section className="receiptGrid large"><div><small>Protected floor</small><strong>₹{state.policy.minimumPayout}</strong><span>No reverse bidding</span></div><div><small>Workload</small><strong>{worker.workloadTodayMinutes}/{workerDailyLimit(worker)} min</strong><span>Hard safety input</span></div><div><small>Valid opportunities</small><strong>{opportunities.length}</strong><span>{opportunities.filter(o=>o.status==="declined").length} safe decline(s)</span></div><div><small>Replayable decisions</small><strong>{receipts.length}</strong><span>Frozen evidence</span></div></section><WorkerReplayCourt state={state} worker={worker} run={run}/></>}
function WorkerIssues({state,worker,job,run}:{state:AppState;worker:Worker;job?:AppState["jobs"][number];run:Run}){const[category,setCategory]=useState("Safety / workability"),relevant=state.issues.filter(i=>i.openedBy===worker.id||Boolean(i.jobId&&state.receipts.some(r=>r.jobId===i.jobId&&(r.workerId===worker.id||r.candidateWorkerIds?.includes(worker.id)))));return <><section className="workerVoiceGuide"><div><p className="eyebrow">WHEN SOMETHING FEELS WRONG</p><h2>Choose the right path.</h2></div><div className="voiceGuideGrid"><article><strong>Current job problem</strong><span>Issue</span><small>Safety, pay, scope or interaction.</small></article><article><strong>Allocation looks wrong</strong><span>Replay Court</span><small>Reproduce frozen decision facts.</small></article><article><strong>Rule should change</strong><span>Speak up</span><small>Suggest a future policy change.</small></article><article><strong>Proposal on ballot</strong><span>Governance</span><small>Review impact, then vote.</small></article></div></section><section className="panel issueComposer"><div><p className="eyebrow">RAISE AN ISSUE</p><h2>Tell the cooperative what needs attention.</h2></div><div className="issueForm"><label className="field"><span>What is it about?</span><select value={category} onChange={e=>setCategory(e.target.value)}><option>Safety / workability</option><option>Payment / payout</option><option>Booked scope</option><option>Service area / travel</option><option>Customer interaction</option><option>Other</option></select></label><button onClick={()=>run(()=>openIssue(state,worker.id,category,job?.id),"Issue sent to cooperative review.")}>Raise issue</button></div></section><section className="panel"><div className="sectionTitle roomy"><div><h2>Your issue register</h2><p className="muted">Issues never automatically change your rating or access to work.</p></div><span>{relevant.length}</span></div>{relevant.length?relevant.map(i=><div className="issueRecord" key={i.id}><div><small>{i.id} · {i.jobId??"General"}</small><strong>{i.category}</strong><span>Status: {i.status}</span>{i.notes.map((n,k)=><p key={k}>{n}</p>)}</div>{i.status!=="closed"&&<button className="secondary" onClick={()=>run(()=>addIssueNote(state,i.id,"I request cooperative review of this record."))}>Add note</button>}</div>):<Empty text="No issue records yet."/>}</section></>}
function WorkerSuggestions({state,worker,run}:{state:AppState;worker:Worker;run:Run}){const mine=state.suggestions.filter(s=>s.workerId===worker.id);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);run(()=>submitPolicySuggestion(state,{category:String(d.get("category")) as SuggestionCategory,title:String(d.get("title")),details:String(d.get("details"))}),"Suggestion submitted. It cannot change policy until review, simulation and voting.");e.currentTarget.reset();}return <><section className="suggestionHero"><div><p className="eyebrow">SPEAK UP</p><h2>Something in the rules doesn’t feel right?</h2><p>Describe it normally. You do not need to write legal or algorithmic policy text.</p></div><span className="supportBadge">Member voice · no retaliation</span></section><section className="suggestionLayout"><form className="panel suggestionForm" onSubmit={submit}><label className="field"><span>Area</span><select name="category" defaultValue="dispatch"><option value="pay">Pay / protection</option><option value="dispatch">Job allocation</option><option value="safety">Workload / safety</option><option value="scope">Scope / change orders</option><option value="access">Opportunity access</option><option value="other">Other</option></select></label><label className="field"><span>Short title</span><input name="title" required/></label><label className="field"><span>What feels wrong, and what would be better?</span><textarea name="details" required/></label><button>Submit suggestion</button></form><section className="panel"><div className="sectionTitle"><h2>Your suggestions</h2><span>{mine.length}</span></div>{mine.length?mine.map(s=><div className="suggestionRow" key={s.id}><span className={`suggestionStatus ${s.status}`}>{pretty(s.status)}</span><strong>{s.title}</strong><span>{pretty(s.category)}</span><p>{s.details}</p></div>):<Empty text="No suggestions yet."/>}</section></section></>}
function WorkerEarnings({state,worker}:{state:AppState;worker:Worker}){const jobs=state.jobs.filter(j=>j.workerId===worker.id),settlements=state.settlements.filter(s=>jobs.some(j=>j.id===s.jobId)),cancellations=state.cancellations.filter(c=>jobs.some(j=>j.id===c.jobId)),total=settlements.reduce((n,s)=>n+s.workerPayout,0)+cancellations.reduce((n,c)=>n+c.workerPayout,0);return <><section className="earningsHero"><div><p className="eyebrow light">PROTECTED EARNINGS</p><h2>₹{total}</h2><p>Settled work and cancellation protections.</p></div></section><section className="panel">{settlements.map(s=><div className="ticket" key={s.id}><div><strong>{s.invoiceNumber}</strong><span>{s.jobId}</span></div><strong>₹{s.workerPayout}</strong></div>)}{cancellations.map(c=><div className="ticket" key={c.id}><div><strong>Cancellation protection</strong><span>{c.jobId}</span></div><strong>₹{c.workerPayout}</strong></div>)}{!settlements.length&&!cancellations.length&&<Empty text="No protected earnings posted yet."/>}</section></>}
function WorkerGovernance({state,worker,run}:{state:AppState;worker:Worker;run:Run}){const proposal=state.proposals.find(p=>p.status==="voting")??state.proposals[0];if(!proposal)return <Empty text="No policy proposal is open."/>;const reviewed=state.policyReviews.some(r=>r.proposalId===proposal.id&&r.memberId===worker.id),voted=state.votes.find(v=>v.proposalId===proposal.id&&v.memberId===worker.id),[reason,setReason]=useState("");return <><section className="governanceHero"><div><p className="eyebrow">ONE MEMBER · ONE VOTE</p><h2>See how the rule affects you before voting.</h2><p>Worker protections sit above ordinary policy votes.</p></div><span className="statusChip">{proposal.status}</span></section><section className="panel policyTwin"><div className="sectionTitle"><h2>{proposal.title}</h2><span>{state.votes.filter(v=>v.proposalId===proposal.id).length}/9 participation</span></div><p>{proposal.description}</p><div className="receiptGrid large"><div><small>Current floor</small><strong>₹{proposal.simulation.currentFloor}</strong></div><div><small>Proposed</small><strong>₹{proposal.simulation.proposedFloor}</strong></div><div><small>Fair wait</small><strong>{proposal.simulation.currentMaxWait} → {proposal.simulation.proposedMaxWait} min</strong></div><div><small>Your current workload</small><strong>{worker.workloadTodayMinutes} min</strong></div></div>{!reviewed&&!voted&&<button onClick={()=>run(()=>reviewPolicyImpact(state,proposal.id,worker.id),"Personal policy impact reviewed. You can now vote.")}>I reviewed my impact</button>}{reviewed&&!voted&&<div className="ballotChoice"><label className="field"><span>Reason if you oppose</span><input value={reason} onChange={e=>setReason(e.target.value)} placeholder="Required only for a No vote"/></label><div className="actions"><button onClick={()=>run(()=>castVote(state,proposal.id,worker.id,"yes"),"Your support vote is recorded once.")}>Vote yes</button><button className="secondary" onClick={()=>run(()=>castVote(state,proposal.id,worker.id,"no",reason),"Your dissent is attached to the ballot.")}>Vote no</button></div></div>}{voted&&<div className="activationDone"><strong>Your vote: {voted.choice.toUpperCase()}</strong><span>{voted.reason||"Recorded once under one-member-one-vote."}</span></div>}</section></>}

function Admin({state,tab,run}:{state:AppState;tab:string;run:Run}){
  if(tab==="workers")return <section className="panel"><div className="sectionTitle roomy"><div><h2>Verified worker-members</h2><p className="muted">Certification and workability are dispatch inputs.</p></div><span>{state.workers.filter(w=>w.verified).length}</span></div><div className="workerTable"><div className="workerTableHead"><span>Member</span><span>Skills</span><span>Workload</span><span>Status</span></div>{state.workers.map(w=><div className="workerRow" key={w.id}><div><strong>{w.name}</strong><small>{w.id} · {state.cooperatives.find(c=>c.id===w.cooperativeId)?.locality}</small></div><span>{w.skills.map(pretty).join(", ")}</span><span>{w.workloadTodayMinutes}/{workerDailyLimit(w)} min</span><span className="statusChip">{w.available&&w.workloadTodayMinutes<workerDailyLimit(w)?"Safe capacity":"Protected / unavailable"}</span></div>)}</div></section>;
  if(tab==="issues")return <><section className="panel"><div className="sectionTitle roomy"><div><h2>Service issues</h2><p className="muted">Human review register; no automatic worker punishment.</p></div><span>{state.issues.length}</span></div>{state.issues.length?state.issues.map(i=><div className="caseRow" key={i.id}><div><small>{i.id} · {i.jobId??"General"}</small><strong>{i.category}</strong><span>Opened by {i.openedBy} · {pretty(i.status)}</span>{i.notes.map((n,k)=><p key={k}>{n}</p>)}</div><div className="actions">{i.status!=="closed"&&<button className="secondary" onClick={()=>run(()=>reviewIssue(state,i.id,"closed"),"Issue closed after cooperative review.")}>Close</button>}</div></div>):<Empty text="No service issues."/>}</section><AdminReplayCourt state={state} run={run}/></>;
  if(tab==="suggestions")return <section className="panel"><div className="sectionTitle roomy"><div><h2>Worker policy suggestions</h2><p className="muted">Accepting a suggestion starts review; it does not activate a rule.</p></div><span>{state.suggestions.length}</span></div>{state.suggestions.length?state.suggestions.map(s=><div className="suggestionRow admin" key={s.id}><span className={`suggestionStatus ${s.status}`}>{pretty(s.status)}</span><strong>{s.title}</strong><span>{s.workerId} · {pretty(s.category)}</span><p>{s.details}</p><div className="actions">{s.status==="submitted"&&<button className="secondary" onClick={()=>run(()=>reviewPolicySuggestion(state,s.id,"under-review"),"Suggestion moved to cooperative review.")}>Review</button>}{["submitted","under-review"].includes(s.status)&&<button onClick={()=>run(()=>reviewPolicySuggestion(state,s.id,"accepted"),"Suggestion accepted for policy development. Active rules did not change.")}>Accept for development</button>}</div></div>):<Empty text="No worker suggestions yet."/>}</section>;
  if(tab==="settlements")return <><section className="metricGrid"><article><small>Settled invoices</small><strong>{state.settlements.length}</strong></article><article><small>Worker payouts</small><strong>₹{state.settlements.reduce((n,s)=>n+s.workerPayout,0)}</strong></article><article><small>Cancellation protection</small><strong>₹{state.cancellations.reduce((n,c)=>n+c.workerPayout,0)}</strong></article><article><small>Platform fee</small><strong>₹0</strong></article></section><section className="panel"><h2>Money register</h2>{state.settlements.map(s=><div className="ticket" key={s.id}><div><strong>{s.invoiceNumber}</strong><span>{s.jobId}</span></div><strong>₹{s.workerPayout}</strong></div>)}{!state.settlements.length&&<Empty text="No settlements yet."/>}</section></>;
  if(tab==="governance")return <AdminGovernance state={state} run={run}/>;
  if(tab==="demand"){const forecast:[string,number][]=[["Mon",46],["Tue",62],["Wed",78],["Thu",55],["Fri",88],["Sat",70],["Sun",50]];return <section className="panel"><div className="sectionTitle roomy"><div><h2>Demand & voluntary workforce planning</h2><p className="muted">Synthetic SIH forecast; never an automatic worker penalty.</p></div><span className="statusChip">Synthetic</span></div><div className="forecast">{forecast.map(([day,height])=><div key={day} style={{height:`${height}%`}}><span>{day}</span><strong>{height}</strong></div>)}</div></section>}
  if(tab==="federation")return <FederationExchange state={state} run={run}/>;
  const open=state.jobs.filter(j=>!["settled","cancelled"].includes(j.status));return <><section className="metricGrid"><article><small>Open jobs</small><strong>{open.length}</strong><span>Shared register</span></article><article><small>Verified members</small><strong>{state.workers.filter(w=>w.verified).length}</strong><span>Live skill registry</span></article><article><small>Open voice records</small><strong>{state.issues.filter(i=>i.status!=="closed").length+state.challenges.filter(c=>c.status!=="closed").length+state.suggestions.filter(s=>["submitted","under-review"].includes(s.status)).length}</strong><span>Issues + Replay + suggestions</span></article><article><small>Protection floor</small><strong>₹{state.policy.minimumPayout}</strong><span>No reverse bidding</span></article></section><section className="panel"><div className="sectionTitle roomy"><div><h2>Live work register</h2><p className="muted">One job state across customer, worker and cooperative.</p></div><span>{state.jobs.length}</span></div>{state.jobs.length?state.jobs.map(j=><div className="ticket" key={j.id}><div><strong>{j.id} · {pretty(j.service)}</strong><span>{j.locality} · {state.cooperatives.find(c=>c.id===j.cooperativeId)?.locality??"routing"} → {state.workers.find(w=>w.id===j.workerId)?.name??"pending"}</span></div><span className="statusChip">{labels[j.status]}</span></div>):<Empty text="No jobs yet. Customer bookings appear here immediately."/>}</section><section className="panel safeguardStrip"><div className="sectionTitle"><h2>Live safeguards</h2><span>Executable</span></div><div><span>Worker Protection Floor</span><span>Dispatch Constitution</span><span>Policy Twin</span><span>Replay Court</span><span>Opportunity Access</span><span>Scope Lock</span><span>Rating Firewall</span><span>Federation Mesh</span></div></section></>;
}
function RuleCompare({state}:{state:AppState}){return <section className="ruleCompare"><div><span>SAME JOBS</span><strong>{state.jobs.length}</strong></div><div><span>SAME WORKERS</span><strong>{state.workers.length}</strong></div><div className="different"><span>DIFFERENT RULES</span><strong>{state.policy.version}</strong></div></section>}
function Empty({text}:{text:string}){return <div className="empty"><span aria-hidden="true">○</span><p>{text}</p></div>}
