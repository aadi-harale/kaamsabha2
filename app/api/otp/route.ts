import { NextResponse } from "next/server";
import { issueOtp, verifyOtp } from "@/lib/otp";
import type { OtpPurpose } from "@/lib/otp";
export const runtime="nodejs";
function secret(){const value=process.env.KAAMSABHA_OTP_SECRET;if(value)return value;if(process.env.NODE_ENV!=="production")return"local-development-only-not-for-production";return""}
function validPurpose(value:unknown):value is OtpPurpose{return value==="start"||value==="completion"}
export async function POST(request:Request){let body:Record<string,unknown>;try{body=await request.json() as Record<string,unknown>}catch{return NextResponse.json({error:"Invalid JSON"},{status:400})}const jobId=typeof body.jobId==="string"?body.jobId:"",purpose=body.purpose;if(!jobId||!validPurpose(purpose))return NextResponse.json({error:"jobId and purpose are required"},{status:400});const key=secret();if(!key)return NextResponse.json({error:"KAAMSABHA_OTP_SECRET is not configured"},{status:503});if(body.action==="issue"){const issued=issueOtp(jobId,purpose,key,Date.now(),5*60_000,crypto.randomUUID());return NextResponse.json({token:issued.token,expiresAt:issued.expiresAt,attemptsLeft:5,demoCode:process.env.KAAMSABHA_DEMO_MODE==="true"?issued.code:undefined})}if(body.action==="verify"){if(typeof body.token!=="string"||typeof body.code!=="string")return NextResponse.json({error:"token and code are required"},{status:400});const result=verifyOtp(body.token,body.code,jobId,purpose,key);return NextResponse.json(result,{status:result.ok?200:400})}return NextResponse.json({error:"Unknown action"},{status:400})}
